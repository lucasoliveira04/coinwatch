spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/coinwatch?sslmode=disable
    username: coinwatch
    password: coinwatch

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true